package cn.edu.yuntukeji.dao;

import org.hibernate.Query;
import org.hibernate.Session;

import cn.edu.yuntukeji.hibernate.HibernateSessionFactory;
import cn.edu.yuntukeji.pojo.Subject;

public class SubjectDAOImpl {
	/*public Subject findSubjectByTitle(String subjectTitle){
		Session session = HibernateSessionFactory.getSession();//获得Session对象
		Query query = session.createQuery("from Subject as where sub.subjectTitle = ?");
		
	}
*/
}
